apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service
  namespace: ztwim-simple
  labels:
    app: data-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
    spec:
      serviceAccountName: data-service
      containers:
        - name: data-service
          image: quay.io/tssc_demos/ztwim-simple:latest
          command: ["python", "/app/data_service.py"]
          ports:
            - containerPort: 8443
              name: mtls
          env:
            - name: ALLOWED_SPIFFE_IDS
              value: "spiffe://idc.com/ns/ztwim-simple/sa/consumer"
            - name: SVID_DIR
              value: "/certs"
          volumeMounts:
            - name: certs
              mountPath: /certs
              readOnly: true
        - name: spiffe-helper
          image: registry.redhat.io/zero-trust-workload-identity-manager/spiffe-helper-rhel9:v0.10.0
          args: ["-config", "/config/helper.conf"]
          volumeMounts:
            - name: spiffe
              mountPath: /spiffe-workload
              readOnly: true
            - name: certs
              mountPath: /certs
            - name: helper-config
              mountPath: /config
              readOnly: true
      volumes:
        - name: spiffe
          csi:
            driver: csi.spiffe.io
            readOnly: true
        - name: certs
          emptyDir: {}
        - name: helper-config
          configMap:
            name: spiffe-helper-config
---
apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: ztwim-simple
spec:
  selector:
    app: data-service
  ports:
    - port: 8443
      targetPort: 8443
      name: mtls
